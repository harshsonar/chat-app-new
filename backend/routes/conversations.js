// Create routing to get all conversations (on left panel)
// localhost/home

const express = require("express");
const router = express.Router();

router.get('', (req, res) => {
    
});

module.exports = router;